# --- Données brutes (volumineuses, publiques, reproductibles depuis le MSSS) ---
00_brut/
Archives/
Demographic Data/
Dépenses par activités/
Dépenses par région/
SAD/
Stats usagers DI-TSA/
Contours financiers/
# fichiers de données à la racine
*.pdf
*.zip
*.xls
*.xlsx
# exception : on garde le catalogue et les sorties canoniques
!00_catalogue/*.csv
!20_canonique/**/*.csv
!20_canonique/**/*.parquet

# exception dans l'exception : le CSV AS478 (~283 Mo) dépasse la limite
# GitHub de 100 Mo/fichier -> on ne versionne que son parquet (mêmes données)
20_canonique/AS478/as478_harmonise.csv

# --- Artefacts reconstructibles ---
__pycache__/
*.pyc
.DS_Store
Thumbs.db

# --- Outils / config locale ---
.claude/

# sauvegardes/partiels de la base (reconstructibles)
40_base/sqdi_sante.db*

# fichier de test technique (non pertinent)
10_scripts/_synctest.txt
