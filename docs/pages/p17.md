---
title: P17 — Attente premier service
---

```js
const meta = await FileAttachment("../data/meta.json").json();
const rawData = await FileAttachment("../data/p17.csv").csv({typed: true});
```

# Page 17 — Usagers différents en attente d'un premier service (au 31 mars)

```js
const etabSelect = view(Inputs.select(
  ["Tout le Québec", ...meta.etablissements.filter(e => e && e.trim())],
  {label: "CISSS / CIUSSS / CRDI", value: "Tout le Québec", width: 400}
));
```

```js
const anneeSelect = view(Inputs.select(
  [...meta.annees].reverse(),
  {label: "Année financière", value: meta.annees[meta.annees.length - 1]}
));
```

```js
function getData(annee, etab) {
  let d = rawData.filter(r => r.Annee === annee);
  if (etab !== "Tout le Québec") d = d.filter(r => r.NomEtablissement === etab);
  const agg = {};
  for (const r of d) { const k = `${r.Ligne}-${r.Colonne}`; agg[k] = (agg[k]||0)+(r.Chiffre||0); }
  return agg;
}
const AGE = {1:'0-4 ans', 2:'5-11 ans', 3:'12-17 ans', 4:'18-21 ans', 5:'22-44 ans', 6:'45-64 ans', 7:'65-74 ans', 8:'75 ans+', 9:'Total'};
function v(data, l, c) { return Math.round(data[`${l}-${c}`] || 0); }
function fmtD(n) { return n > 0 ? n.toFixed(1) : "—"; }
function fmt(n) { return n.toLocaleString('fr-CA'); }
const data = getData(anneeSelect, etabSelect);
```

## Déficience intellectuelle (DI)

```js
const DI = [
  {ligne:1, label:"Nombre de personnes"},
  {ligne:2, label:"Délai moyen (jours)"},
  {ligne:3, label:"Délai médian (jours)"},
];
view(Inputs.table(DI.map(r => ({
  "Indicateur": r.label,
  ...Object.fromEntries([1,2,3,4,5,6,7,8].map(c => [AGE[c], r.ligne === 1 ? fmt(v(data,r.ligne,c)) : fmtD(v(data,r.ligne,c))])),
  "Total": r.ligne === 1 ? fmt(v(data,r.ligne,9)) : fmtD(v(data,r.ligne,9))
})), {width:"100%"}));
```

## Trouble du spectre de l'autisme (TSA)

```js
const TSA = [
  {ligne:4, label:"Nombre de personnes"},
  {ligne:5, label:"Délai moyen (jours)"},
  {ligne:6, label:"Délai médian (jours)"},
];
view(Inputs.table(TSA.map(r => ({
  "Indicateur": r.label,
  ...Object.fromEntries([1,2,3,4,5,6,7,8].map(c => [AGE[c], r.ligne === 4 ? fmt(v(data,r.ligne,c)) : fmtD(v(data,r.ligne,c))])),
  "Total": r.ligne === 4 ? fmt(v(data,r.ligne,9)) : fmtD(v(data,r.ligne,9))
})), {width:"100%"}));
```

## Total DITSA

```js
const TOT = [
  {ligne:7, label:"Nombre de personnes (L.1+L.4)"},
  {ligne:8, label:"Délai moyen — Total"},
  {ligne:9, label:"Délai médian — Total"},
];
view(Inputs.table(TOT.map(r => ({
  "Indicateur": r.label,
  ...Object.fromEntries([1,2,3,4,5,6,7,8].map(c => [AGE[c], r.ligne === 7 ? fmt(v(data,r.ligne,c)) : fmtD(v(data,r.ligne,c))])),
  "Total": r.ligne === 7 ? fmt(v(data,r.ligne,9)) : fmtD(v(data,r.ligne,9))
})), {width:"100%"}));
```

## Évolution — Nombre de personnes en attente d'un premier service

```js
const trendData = meta.annees.map(annee => {
  const d = (() => {
    let rows = rawData.filter(r => r.Annee === annee);
    if (etabSelect !== "Tout le Québec") rows = rows.filter(r => r.NomEtablissement === etabSelect);
    const agg = {};
    for (const r of rows) { const k = `${r.Ligne}-${r.Colonne}`; agg[k] = (agg[k]||0)+(r.Chiffre||0); }
    return agg;
  })();
  return {annee, di: v(d,1,9), tsa: v(d,4,9), total: v(d,7,9)};
}).filter(d => d.total > 0);

Plot.plot({
  title: `Attente premier service — ${etabSelect}`,
  x: {label: "Année", tickRotate: -30},
  y: {label: "Personnes en attente", grid: true},
  color: {legend: true},
  marks: [
    Plot.line(trendData, {x:"annee", y:"di", stroke:"#378ADD", strokeWidth:2, marker:"circle"}),
    Plot.line(trendData, {x:"annee", y:"tsa", stroke:"#1D9E75", strokeWidth:2, marker:"circle"}),
    Plot.line(trendData, {x:"annee", y:"total", stroke:"#D85A30", strokeWidth:2.5, strokeDasharray:"4,3", marker:"circle"}),
    Plot.ruleY([0]),
  ],
  width: 700, height: 300,
})
```

*Bleu : DI | Vert : TSA | Orange pointillé : Total*

<div style="font-size:12px; color: #888; margin-top: 2rem;">
Source : MSSS Québec — Rapport AS485, Page 17. Données compilées de 2010-2011 à 2024-2025.
</div>
