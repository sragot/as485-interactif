---
title: P09 — Clientèle desservie (externe)
---

```js
import {tableWithTools} from "../components/tableUtils.js";

const meta = await FileAttachment("../data/meta.json").json();
const rawData = await FileAttachment("../data/p09.csv").csv({typed: true});
```

# Page 9 — Répartition des usagers desservis en externe par s-c/a (du 1er avril au 31 mars)

```js
const etabSelect = view(Inputs.select(
  ["Tout le Québec", ...meta.etablissements.filter(e => e && e.trim())],
  {label: "CISSS / CIUSSS / CRDI", value: "Tout le Québec", width: 400}
));
```

```js
const anneeSelect = view(Inputs.select(
  [...meta.annees].reverse(),
  {label: "Année financière", value: meta.annees[meta.annees.length - 1]}
));
```

```js
function getData(annee, etab) {
  let d = rawData.filter(r => r.Annee === annee);
  if (etab !== "Tout le Québec") d = d.filter(r => r.NomEtablissement === etab);
  const agg = {};
  for (const r of d) {
    const key = `${r.Ligne}-${r.Colonne}`;
    agg[key] = (agg[key] || 0) + (r.Chiffre || 0);
  }
  return agg;
}

const AGE = {1:'0-4 ans', 2:'5-11 ans', 3:'12-17 ans', 4:'18-21 ans', 5:'22-44 ans', 6:'45-64 ans', 7:'65-74 ans', 8:'75 ans+', 9:'Total'};
const ICI = {1:'1 an', 2:'2 ans', 3:'3 ans', 4:'4 ans', 5:'5 ans', 9:'Total (C.1–C.5)'};
function v(data, l, c) { return Math.round(data[`${l}-${c}`] || 0); }
function fmt(n) { return n.toLocaleString('fr-CA'); }

const data = getData(anneeSelect, etabSelect);
```

---

## Services d'adaptation et de réadaptation à la personne — DITSA (s-c/a 8050)

```js
const DITSA = [
  {ligne: 1, label: "Usagers différents desservis durant l'année"},
  {ligne: 2, label: "Départs durant l'année"},
  {ligne: 3, label: "Nombre total de HPS"},
];
const tableDITSA = DITSA.map(r => ({
  "Indicateur": r.label,
  ...Object.fromEntries([1,2,3,4,5,6,7,8].map(c => [AGE[c], fmt(v(data, r.ligne, c))])),
  "Total": fmt(v(data, r.ligne, 9))
}));
view(tableWithTools(Inputs.table(tableDITSA, {width: "100%"}), tableDITSA, "p09_ditsa.csv"));
```

## Services d'adaptation et de réadaptation à la personne — DI (s-c/a 8051)

```js
const DI_ROWS = [
  {ligne: 4, label: "Usagers desservis durant l'année"},
  {ligne: 5, label: "Départs durant l'année"},
  {ligne: 6, label: "Nombre total de HPS"},
];
const tableDI = DI_ROWS.map(r => ({
  "Indicateur": r.label,
  ...Object.fromEntries([1,2,3,4,5,6,7,8].map(c => [AGE[c], fmt(v(data, r.ligne, c))])),
  "Total": fmt(v(data, r.ligne, 9))
}));
view(tableWithTools(Inputs.table(tableDI, {width: "100%"}), tableDI, "p09_di.csv"));
```

## Services d'intervention comportementale intensive — TSA (s-c/a 8052)

_Colonnes : durée du suivi ICI (1 an à 5 ans)_

```js
const ICI_ROWS = [
  {ligne: 7,  label: "Enfants desservis durant l'année"},
  {ligne: 8,  label: "Nombre d'heures totales d'intervention comportementale intensive"},
  {ligne: 9,  label: "Nombre de semaines totales d'intervention comportementale intensive"},
  {ligne: 10, label: "Départs durant l'année"},
];
const tableICI = ICI_ROWS.map(r => ({
  "Indicateur": r.label,
  ...Object.fromEntries([1,2,3,4,5].map(c => [ICI[c], fmt(v(data, r.ligne, c))])),
  [ICI[9]]: fmt(v(data, r.ligne, 9))
}));
view(tableWithTools(Inputs.table(tableICI, {width: "100%"}), tableICI, "p09_ici_tsa.csv"));
```

## Services d'adaptation et de réadaptation à la personne — TSA (s-c/a 8053)

```js
const TSA_ROWS = [
  {ligne: 11, label: "Usagers desservis durant l'année"},
  {ligne: 12, label: "Départs durant l'année"},
  {ligne: 13, label: "Nombre total de HPS"},
];
const tableTSA = TSA_ROWS.map(r => ({
  "Indicateur": r.label,
  ...Object.fromEntries([1,2,3,4,5,6,7,8].map(c => [AGE[c], fmt(v(data, r.ligne, c))])),
  "Total": fmt(v(data, r.ligne, 9))
}));
view(tableWithTools(Inputs.table(tableTSA, {width: "100%"}), tableTSA, "p09_tsa.csv"));
```

---

## Graphique — Usagers desservis par service et groupe d'âge

```js
const chartData = [1,2,3,4,5,6,7,8].flatMap(c => [
  {age: AGE[c], service: "DITSA (8050)", n: v(data, 1, c)},
  {age: AGE[c], service: "DI (8051)",    n: v(data, 4, c)},
  {age: AGE[c], service: "TSA (8053)",   n: v(data, 11, c)},
]);

Plot.plot({
  title: `Usagers desservis en externe — ${etabSelect} — ${anneeSelect}`,
  x: {label: "Groupe d'âge"},
  y: {label: "Usagers"},
  color: {legend: true, domain: ["DITSA (8050)", "DI (8051)", "TSA (8053)"], range: ["#534AB7", "#378ADD", "#1D9E75"]},
  marks: [
    Plot.barY(chartData, {x: "age", y: "n", fill: "service", title: d => `${d.service}\n${d.age}: ${d.n.toLocaleString('fr-CA')}`}),
    Plot.ruleY([0]),
  ],
  width: 700,
})
```

<div style="font-size:12px; color: #888; margin-top: 2rem;">
Source : MSSS Québec — Rapport AS485, Page 09. Données compilées de 2010-2011 à 2024-2025.
</div>
