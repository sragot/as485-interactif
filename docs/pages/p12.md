---
title: P12 — Intégration communautaire
---

```js
import {tableWithTools} from "../components/tableUtils.js";

const meta = await FileAttachment("../data/meta.json").json();
const rawData = await FileAttachment("../data/p12.csv").csv({typed: true});
```

# Page 12 — Usagers inscrits aux services d'adaptation et de réadaptation en contexte d'intégration communautaire-DITSA (s-c/a 7001) (du 1er avril au 31 mars)

```js
const etabSelect = view(Inputs.select(
  ["Tout le Québec", ...meta.etablissements.filter(e => e && e.trim())],
  {label: "CISSS / CIUSSS / CRDI", value: "Tout le Québec", width: 400}
));
```

```js
const anneeSelect = view(Inputs.select(
  [...meta.annees].reverse(),
  {label: "Année financière", value: meta.annees[meta.annees.length - 1]}
));
```

```js
function getData(annee, etab) {
  let d = rawData.filter(r => r.Annee === annee);
  if (etab !== "Tout le Québec") d = d.filter(r => r.NomEtablissement === etab);
  const agg = {};
  for (const r of d) { const k = `${r.Ligne}-${r.Colonne}`; agg[k] = (agg[k]||0)+(r.Chiffre||0); }
  return agg;
}
const AGE = {1:'0-4 ans', 2:'5-11 ans', 3:'12-17 ans', 4:'18-21 ans', 5:'22-44 ans', 6:'45-64 ans', 7:'65-74 ans', 8:'75 ans+', 9:'Total'};
function v(data, l, c) { return Math.round(data[`${l}-${c}`] || 0); }
function fmt(n) { return n.toLocaleString('fr-CA'); }
const data = getData(anneeSelect, etabSelect);
```

## Déficience intellectuelle (DI)

```js
const DI = [{ligne:1,label:"Usagers différents desservis"},{ligne:2,label:"Départs"},{ligne:3,label:"Nombre de présences"}];
const tableDI = DI.map(r => ({
  "Indicateur": r.label,
  ...Object.fromEntries([1,2,3,4,5,6,7,8].map(c => [AGE[c], fmt(v(data,r.ligne,c))])),
  "Total": fmt(v(data,r.ligne,9))
}));
view(tableWithTools(Inputs.table(tableDI, {width:"100%"}), tableDI, "p12_di.csv"));
```

## Trouble du spectre de l'autisme (TSA)

```js
const TSA = [{ligne:4,label:"Usagers différents desservis"},{ligne:5,label:"Départs"},{ligne:6,label:"Nombre de présences"}];
const tableTSA = TSA.map(r => ({
  "Indicateur": r.label,
  ...Object.fromEntries([1,2,3,4,5,6,7,8].map(c => [AGE[c], fmt(v(data,r.ligne,c))])),
  "Total": fmt(v(data,r.ligne,9))
}));
view(tableWithTools(Inputs.table(tableTSA, {width:"100%"}), tableTSA, "p12_tsa.csv"));
```

## Total DITSA

```js
const TOTAL = [{ligne:7,label:"Usagers différents desservis (L.1+L.4)"},{ligne:8,label:"Départs (L.2+L.5)"},{ligne:9,label:"Nombre de présences (L.3+L.6)"}];
const tableTotal = TOTAL.map(r => ({
  "Indicateur": r.label,
  ...Object.fromEntries([1,2,3,4,5,6,7,8].map(c => [AGE[c], fmt(v(data,r.ligne,c))])),
  "Total": fmt(v(data,r.ligne,9))
}));
view(tableWithTools(Inputs.table(tableTotal, {width:"100%"}), tableTotal, "p12_total.csv"));
```

<div style="font-size:12px; color: #888; margin-top: 2rem;">
Source : MSSS Québec — Rapport AS485, Page 12. Données compilées de 2010-2011 à 2024-2025.
</div>
