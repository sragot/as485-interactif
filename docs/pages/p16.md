---
title: P16-17 — Liste d'attente
---

```js
import {tableWithTools} from "../components/tableUtils.js";

const meta = await FileAttachment("../data/meta.json").json();
const rawP16 = await FileAttachment("../data/p16.csv").csv({typed: true});
const rawP17 = await FileAttachment("../data/p17.csv").csv({typed: true});
```

# Pages 16-17 — Liste d'attente

```js
const etabSelect = view(Inputs.select(
  ["Tout le Québec", ...meta.etablissements.filter(e => e && e.trim())],
  {label: "CISSS / CIUSSS / CRDI", value: "Tout le Québec", width: 400}
));
```

```js
const anneeSelect = view(Inputs.select(
  [...meta.annees].reverse(),
  {label: "Année financière", value: meta.annees[meta.annees.length - 1]}
));
```

```js
function getData(raw, annee, etab) {
  let d = raw.filter(r => r.Annee === annee);
  if (etab !== "Tout le Québec") d = d.filter(r => r.NomEtablissement === etab);
  const agg = {};
  for (const r of d) {
    const key = `${r.Ligne}-${r.Colonne}`;
    agg[key] = (agg[key] || 0) + (r.Chiffre || 0);
  }
  return agg;
}

const AGE = {1:'0-4 ans', 2:'5-11 ans', 3:'12-17 ans', 4:'18-21 ans', 5:'22-44 ans', 6:'45-64 ans', 7:'65-74 ans', 8:'75 ans+', 9:'Total'};
function v(data, l, c) { return Math.round(data[`${l}-${c}`] || 0); }
function fmt(n) { return n.toLocaleString('fr-CA'); }

const d16 = getData(rawP16, anneeSelect, etabSelect);
const d17 = getData(rawP17, anneeSelect, etabSelect);
```

---

## Page 16 — Demandes de services reçues

```js
const ROWS16 = [
  { ligne: 1, label: "Reçues — Déficience intellectuelle (DI)" },
  { ligne: 2, label: "Reçues — TSA" },
  { ligne: 3, label: "Reçues — TOTAL (L.1 + L.2)" },
  { ligne: 4, label: "Reçues dont services débutés — DI" },
  { ligne: 5, label: "Reçues dont services débutés — TSA" },
  { ligne: 6, label: "Reçues dont services débutés — TOTAL" },
  { ligne: 7, label: "Fermées — DI" },
  { ligne: 8, label: "Fermées — TSA" },
  { ligne: 9, label: "Fermées — TOTAL" },
];

const table16 = ROWS16.map(r => ({
  "Indicateur": r.label,
  ...Object.fromEntries([1,2,3,4,5,6,7,8].map(c => [AGE[c], fmt(v(d16, r.ligne, c))])),
  "Total": fmt(v(d16, r.ligne, 9)),
}));
view(tableWithTools(Inputs.table(table16, {width: "100%"}), table16, "p16_demandes.csv"));
```

---

## Page 17 — Usagers différents en attente

```js
const ROWS17 = [
  { ligne: 1, label: "Nb de personnes en attente — DI" },
  { ligne: 2, label: "Nb de personnes en attente — TSA" },
  { ligne: 3, label: "Nb de personnes en attente — TOTAL" },
];

const table17 = ROWS17.map(r => ({
  "Indicateur": r.label,
  ...Object.fromEntries([1,2,3,4,5,6,7,8].map(c => [AGE[c], fmt(v(d17, r.ligne, c))])),
  "Total": fmt(v(d17, r.ligne, 9)),
}));
view(tableWithTools(Inputs.table(table17, {width: "100%"}), table17, "p17_attente.csv"));
```

---

## Évolution de la liste d'attente — Toutes années

```js
const allYears = meta.annees;
const trendData = [];

for (const annee of allYears) {
  const dd = getData(rawP17, annee, etabSelect);
  const total = v(dd, 3, 9);
  const di = v(dd, 1, 9);
  const tsa = v(dd, 2, 9);
  if (total > 0) {
    trendData.push({ annee, total, di, tsa });
  }
}

// Aplatir pour canal couleur avec légende fonctionnelle
const trendFlat = trendData.flatMap(d => [
  {annee: d.annee, serie: "DI", val: d.di},
  {annee: d.annee, serie: "TSA", val: d.tsa},
  {annee: d.annee, serie: "Total", val: d.total},
]);

Plot.plot({
  title: `Évolution liste d'attente — ${etabSelect}`,
  x: {label: "Année", tickRotate: -30},
  y: {label: "Personnes en attente", grid: true},
  color: {legend: true, domain: ["DI", "TSA", "Total"], range: ["#378ADD", "#1D9E75", "#D85A30"]},
  marks: [
    Plot.line(trendFlat.filter(d => d.serie !== "Total"), {x: "annee", y: "val", stroke: "serie", strokeWidth: 2, marker: "circle"}),
    Plot.line(trendFlat.filter(d => d.serie === "Total"), {x: "annee", y: "val", stroke: "serie", strokeWidth: 2.5, strokeDasharray: "4,3", marker: "circle"}),
    Plot.ruleY([0]),
  ],
  width: 700,
  height: 320,
})
```

<div style="font-size:12px; color: #888; margin-top: 2rem;">
Source : MSSS Québec — Rapport AS485, Pages 16-17. Données compilées de 2010-2011 à 2024-2025.
</div>
