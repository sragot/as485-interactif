---
title: P05 — Mouvement des usagers en internat
---

```js
import {tableWithTools} from "../components/tableUtils.js";

const meta = await FileAttachment("../data/meta.json").json();
const rawData = await FileAttachment("../data/p05.csv").csv({typed: true});
```

# Page 5 — Mouvement des usagers admis en internat DITSA par groupe d'âge

```js
const etabSelect = view(Inputs.select(
  ["Tout le Québec", ...meta.etablissements.filter(e => e && e.trim())],
  {label: "CISSS / CIUSSS / CRDI", value: "Tout le Québec", width: 400}
));
```

```js
const anneeSelect = view(Inputs.select(
  [...meta.annees].reverse(),
  {label: "Année financière", value: meta.annees[meta.annees.length - 1]}
));
```

```js
const sousPageSelect = view(Inputs.select(
  [{value: 0, label: "P05-0 — Sommaire DITSA"}, {value: 1, label: "P05-1 — DI seulement"}, {value: 2, label: "P05-2 — TSA seulement"}],
  {label: "Sous-page", value: 0}
));
```

```js
function getData(annee, etab, sousPage) {
  let d = rawData.filter(r => r.Annee === annee && Number(r.SousPage || 0) === sousPage);
  if (etab !== "Tout le Québec") d = d.filter(r => r.NomEtablissement === etab);
  const agg = {};
  for (const r of d) {
    const key = `${r.Ligne}-${r.Colonne}`;
    agg[key] = (agg[key] || 0) + (r.Chiffre || 0);
  }
  return agg;
}

const AGE = {1:'0-4 ans', 2:'5-11 ans', 3:'12-17 ans', 4:'18-21 ans', 5:'22-44 ans', 6:'45-64 ans', 7:'65-74 ans', 8:'75 ans+', 9:'Total'};
function v(data, l, c) { return Math.round(data[`${l}-${c}`] || 0); }
function fmt(n) { return n.toLocaleString('fr-CA'); }
const data = getData(anneeSelect, etabSelect, sousPageSelect.value);
```

## Mouvement des usagers — par groupe d'âge

```js
const MVMT = [
  {ligne: 1, label: "Usagers au début de l'année"},
  {ligne: 2, label: "Admissions durant l'année"},
  {ligne: 3, label: "Sous-total (L.1 + L.2)"},
  {ligne: 4, label: "Départs durant l'année"},
  {ligne: 5, label: "Décès"},
  {ligne: 6, label: "Sous-total départs (L.4 + L.5)"},
  {ligne: 7, label: "Usagers à la fin de l'année"},
];

const tableMvmt = MVMT.map(r => ({
  "Indicateur": r.label,
  ...Object.fromEntries([1,2,3,4,5,6,7,8].map(c => [AGE[c], fmt(v(data,r.ligne,c))])),
  "Total": fmt(v(data, r.ligne, 9))
}));
view(tableWithTools(Inputs.table(tableMvmt, {width: "100%"}), tableMvmt, "p05_mouvement.csv"));
```

## Classement des départs selon la durée du séjour

```js
const DUREE = [
  {ligne: 8, label: "0 à 45 jours"},
  {ligne: 9, label: "46 à 90 jours"},
  {ligne: 10, label: "91 à 180 jours"},
  {ligne: 11, label: "181 à 365 jours"},
  {ligne: 12, label: "366 à 730 jours"},
  {ligne: 13, label: "731 jours et plus"},
  {ligne: 14, label: "Total"},
];

const tableDuree = DUREE.map(r => ({
  "Durée du séjour": r.label,
  ...Object.fromEntries([1,2,3,4,5,6,7,8].map(c => [AGE[c], fmt(v(data,r.ligne,c))])),
  "Total": fmt(v(data, r.ligne, 9))
}));
view(tableWithTools(Inputs.table(tableDuree, {width: "100%"}), tableDuree, "p05_duree.csv"));
```

## Orientation au départ

```js
const ORIENT = [
  {ligne: 16, label: "Milieu de vie naturel"},
  {ligne: 17, label: "Ressource intermédiaire (RI)"},
  {ligne: 18, label: "Ressource de type familial (RTF)"},
  {ligne: 19, label: "RRAC"},
  {ligne: 20, label: "RRAAC"},
  {ligne: 21, label: "Autre RNI gérée par CR"},
  {ligne: 22, label: "RNI gérée par autre établissement"},
  {ligne: 23, label: "Centre hospitalier"},
  {ligne: 24, label: "Hébergement en CHSLD"},
  {ligne: 25, label: "Internat"},
  {ligne: 26, label: "Foyer de groupe"},
  {ligne: 27, label: "Autre CR"},
  {ligne: 28, label: "Milieu résidentiel non identifié"},
  {ligne: 29, label: "Autre"},
  {ligne: 30, label: "TOTAL"},
];

const chartOrient = ORIENT.filter(r => r.ligne !== 30).map(r => ({
  orientation: r.label,
  departs: v(data, r.ligne, 1),
})).filter(d => d.departs > 0).sort((a,b) => b.departs - a.departs);

const tableOrient = ORIENT.map(r => ({
  "Orientation": r.label,
  "Nombre de départs": fmt(v(data, r.ligne, 1)),
}));
view(tableWithTools(Inputs.table(tableOrient, {width: "100%"}), tableOrient, "p05_orientation.csv"));
```

<div style="font-size:12px; color: #888; margin-top: 2rem;">
Source : MSSS Québec — Rapport AS485, Pages 05-0, 05-1, 05-2. Données compilées de 2010-2011 à 2024-2025.
</div>
