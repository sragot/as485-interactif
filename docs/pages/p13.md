---
title: P13 — Emploi et occupation
---

```js
import {tableWithTools} from "../components/tableUtils.js";

const meta = await FileAttachment("../data/meta.json").json();
const rawData = await FileAttachment("../data/p13.csv").csv({typed: true});
```

# Page 13 — Emploi et occupation (DI-TSA)

```js
const etabSelect = view(Inputs.select(
  ["Tout le Québec", ...meta.etablissements.filter(e => e && e.trim())],
  {label: "CISSS / CIUSSS / CRDI", value: "Tout le Québec", width: 400}
));
```

```js
const anneeSelect = view(Inputs.select(
  [...meta.annees].reverse(),
  {label: "Année financière", value: meta.annees[meta.annees.length - 1]}
));
```

```js
function getData(annee, etab) {
  let d = rawData.filter(r => r.Annee === annee);
  if (etab !== "Tout le Québec") d = d.filter(r => r.NomEtablissement === etab);
  const agg = {};
  for (const r of d) {
    const key = `${r.Ligne}-${r.Colonne}`;
    agg[key] = (agg[key] || 0) + (r.Chiffre || 0);
  }
  return agg;
}
const AGE = {1:'0-4 ans', 2:'5-11 ans', 3:'12-17 ans', 4:'18-21 ans', 5:'22-44 ans', 6:'45-64 ans', 7:'65-74 ans', 8:'75 ans+', 9:'Total'};
function v(data, l, c) { return Math.round(data[`${l}-${c}`] || 0); }
function fmt(n) { return n.toLocaleString('fr-CA'); }
const data = getData(anneeSelect, etabSelect);
```

## Atelier de travail — DITSA

```js
const AT = [
  {ligne:1, label:"Usagers desservis durant l'année"},
  {ligne:2, label:"— Masculin"}, {ligne:3, label:"— Féminin"},
  {ligne:4, label:"Départs durant l'année"},
  {ligne:5, label:"Nombre de présences"},
];
const tableAT = AT.map(r => ({
  "Indicateur": r.label,
  ...Object.fromEntries([1,2,3,4,5,6,7,8].map(c => [AGE[c], fmt(v(data,r.ligne,c))])),
  "Total": fmt(v(data,r.ligne,9))
}));
view(tableWithTools(Inputs.table(tableAT, {width:"100%"}), tableAT, "p13_atelier.csv"));
```

## Stages individuels en milieu de travail — DI

```js
const ST = [
  {ligne:6, label:"Usagers desservis durant l'année"},
  {ligne:7, label:"— Masculin"}, {ligne:8, label:"— Féminin"},
  {ligne:9, label:"Départs durant l'année"}, {ligne:10, label:"Nombre de présences"},
];
const tableST = ST.map(r => ({
  "Indicateur": r.label,
  ...Object.fromEntries([1,2,3,4,5,6,7,8].map(c => [AGE[c], fmt(v(data,r.ligne,c))])),
  "Total": fmt(v(data,r.ligne,9))
}));
view(tableWithTools(Inputs.table(tableST, {width:"100%"}), tableST, "p13_stages.csv"));
```

## Plateaux de travail — DITSA

```js
const PT = [
  {ligne:11, label:"Usagers desservis durant l'année"},
  {ligne:12, label:"— Masculin"}, {ligne:13, label:"— Féminin"},
  {ligne:14, label:"Départs durant l'année"}, {ligne:15, label:"Nombre de présences"},
];
const tablePT = PT.map(r => ({
  "Indicateur": r.label,
  ...Object.fromEntries([1,2,3,4,5,6,7,8].map(c => [AGE[c], fmt(v(data,r.ligne,c))])),
  "Total": fmt(v(data,r.ligne,9))
}));
view(tableWithTools(Inputs.table(tablePT, {width:"100%"}), tablePT, "p13_plateaux.csv"));
```

<div style="font-size:12px; color: #888; margin-top: 2rem;">
Source : MSSS Québec — Rapport AS485, Page 13. Données compilées de 2010-2011 à 2024-2025.
</div>
