---
title: P14 — Provenance régionale
---

```js
const meta = await FileAttachment("../data/meta.json").json();
const rawData = await FileAttachment("../data/p14.csv").csv({typed: true});
```

# Page 14 — Répartition des usagers ayant des services d'intégration au travail selon la provenance régionale

```js
const etabSelect = view(Inputs.select(
  ["Tout le Québec", ...meta.etablissements.filter(e => e && e.trim())],
  {label: "CISSS / CIUSSS / CRDI", value: "Tout le Québec", width: 400}
));
```

```js
const anneeSelect = view(Inputs.select(
  [...meta.annees].reverse(),
  {label: "Année financière", value: meta.annees[meta.annees.length - 1]}
));
```

```js
function getData(annee, etab) {
  let d = rawData.filter(r => r.Annee === annee);
  if (etab !== "Tout le Québec") d = d.filter(r => r.NomEtablissement === etab);
  const agg = {};
  for (const r of d) { const k = `${r.Ligne}-${r.Colonne}`; agg[k] = (agg[k]||0)+(r.Chiffre||0); }
  return agg;
}
function v(data, l, c) { return Math.round(data[`${l}-${c}`] || 0); }
function fmt(n) { return n.toLocaleString('fr-CA'); }
const data = getData(anneeSelect, etabSelect);
```

## Par région de provenance

```js
const REGIONS = [
  {ligne:1, label:"01 — Bas-Saint-Laurent"},
  {ligne:2, label:"02 — Saguenay–Lac-Saint-Jean"},
  {ligne:3, label:"03 — Capitale-Nationale"},
  {ligne:4, label:"04 — Mauricie et Centre-du-Québec"},
  {ligne:5, label:"05 — Estrie"},
  {ligne:6, label:"06 — Montréal"},
  {ligne:7, label:"07 — Outaouais"},
  {ligne:8, label:"08 — Abitibi-Témiscamingue"},
  {ligne:9, label:"09 — Côte-Nord"},
  {ligne:10, label:"10 — Nord-du-Québec"},
  {ligne:11, label:"11 — Gaspésie–Îles-de-la-Madeleine"},
  {ligne:12, label:"12 — Chaudière-Appalaches"},
  {ligne:13, label:"13 — Laval"},
  {ligne:14, label:"14 — Lanaudière"},
  {ligne:15, label:"15 — Laurentides"},
  {ligne:16, label:"16 — Montérégie"},
  {ligne:17, label:"17 — Nunavik"},
  {ligne:18, label:"18 — Terres-Cries-de-la-Baie-James"},
  {ligne:19, label:"Sous-total (L.1 à L.18)"},
  {ligne:20, label:"Hors Québec"},
  {ligne:21, label:"TOTAL"},
];

// Colonnes: 1=Intégration communautaire usagers, 2=présences, 3=Atelier travail usagers, 4=présences, 5=Stages usagers, 6=présences, 7=Plateaux usagers, 8=présences, 9=Intégration emploi
view(Inputs.table(REGIONS.map(r => ({
  "Région": r.label,
  "Int. communautaire (usagers)": fmt(v(data,r.ligne,1)),
  "Atelier travail (usagers)": fmt(v(data,r.ligne,3)),
  "Stages individuels (usagers)": fmt(v(data,r.ligne,5)),
  "Plateaux de travail (usagers)": fmt(v(data,r.ligne,7)),
  "Intégration emploi (usagers)": fmt(v(data,r.ligne,9)),
})), {width:"100%"}));
```

<div style="font-size:12px; color: #888; margin-top: 2rem;">
Source : MSSS Québec — Rapport AS485, Page 14. Données compilées de 2010-2011 à 2024-2025.
</div>
