---
title: P23 — Répartition par déficience
---

```js
import {tableWithTools} from "../components/tableUtils.js";

const meta = await FileAttachment("../data/meta.json").json();
const rawData = await FileAttachment("../data/p23.csv").csv({typed: true});
```

# Page 23 — Répartition des usagers différents desservis par groupe d'âge et par déficience pour tous les c/a (du 1er avril au 31 mars)

```js
const etabSelect = view(Inputs.select(
  ["Tout le Québec", ...meta.etablissements.filter(e => e && e.trim())],
  {label: "CISSS / CIUSSS / CRDI", value: "Tout le Québec", width: 400}
));
```

```js
const anneeSelect = view(Inputs.select(
  [...meta.annees].reverse(),
  {label: "Année financière", value: meta.annees[meta.annees.length - 1]}
));
```

```js
function getData(annee, etab) {
  let d = rawData.filter(r => r.Annee === annee);
  if (etab !== "Tout le Québec") d = d.filter(r => r.NomEtablissement === etab);
  const agg = {};
  for (const r of d) {
    const key = `${r.Ligne}-${r.Colonne}`;
    agg[key] = (agg[key] || 0) + (r.Chiffre || 0);
  }
  return agg;
}
const AGE = {1:'0-4 ans', 2:'5-11 ans', 3:'12-17 ans', 4:'18-21 ans', 5:'22-44 ans', 6:'45-64 ans', 7:'65-74 ans', 8:'75 ans+', 9:'Total'};
function v(data, l, c) { return Math.round(data[`${l}-${c}`] || 0); }
function fmt(n) { return n.toLocaleString('fr-CA'); }
const data = getData(anneeSelect, etabSelect);
```

## Répartition des usagers différents par type de déficience

```js
const ROWS = [
  {ligne:1, label:"Déficience intellectuelle (DI)"},
  {ligne:2, label:"Trouble du spectre de l'autisme (TSA)"},
  {ligne:3, label:"TOTAL (L.1 + L.2)"},
];
const tableRows = ROWS.map(r => ({
  "Déficience": r.label,
  ...Object.fromEntries([1,2,3,4,5,6,7,8].map(c => [AGE[c], fmt(v(data,r.ligne,c))])),
  "Total": fmt(v(data,r.ligne,9))
}));
view(tableWithTools(Inputs.table(tableRows, {width:"100%"}), tableRows, "p23_deficience.csv"));
```

## Graphique — Répartition DI vs TSA par groupe d'âge

```js
const chartData = [1,2,3,4,5,6,7,8].flatMap(c => [
  {age: AGE[c], type: "DI", n: v(data, 1, c)},
  {age: AGE[c], type: "TSA", n: v(data, 2, c)},
]);

Plot.plot({
  title: `DI vs TSA par groupe d'âge — ${etabSelect} — ${anneeSelect}`,
  x: {label: "Groupe d'âge"},
  y: {label: "Usagers différents"},
  color: {legend: true, domain: ["DI","TSA"], range: ["#378ADD","#1D9E75"]},
  marks: [
    Plot.barY(chartData, {x: "age", y: "n", fill: "type", fx: "type"}),
    Plot.ruleY([0]),
  ],
  width: 700, height: 280,
})
```

<div style="font-size:12px; color: #888; margin-top: 2rem;">
Source : MSSS Québec — Rapport AS485, Page 23. Données compilées de 2010-2011 à 2024-2025.
</div>
